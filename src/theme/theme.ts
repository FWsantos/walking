import {DefaultTheme} from 'react-native-paper';

const theme = {
    ...DefaultTheme,
    // roundness: 3,
    // colors: {
    //   ...DefaultTheme.colors,
    //   primary: 'tomato',
    //   accent: 'yellow',
    // },
  };

export default theme;